let defaultSymbol = { color: "#22ffff", weight: 1, fillColor: "#28ffff", opacity: .75 }, currentSymbol = null, displayPolygonLayerGroup = null; export const _setCurrentSymbol = o => { currentSymbol = JSON.parse(o) }; let _polygons = []; export const _addPolygonsArrayToMap = (o, l, r) => { null === currentSymbol && (currentSymbol = defaultSymbol); var y = JSON.parse(o.toString()); if (null === displayPolygonLayerGroup) { displayPolygonLayerGroup = r.featureGroup().addTo(l); let e = r.polygon(y, currentSymbol); displayPolygonLayerGroup.addLayer(e), _polygons.push(e) } else { let n = r.polygon(y, currentSymbol); displayPolygonLayerGroup.addLayer(n), _polygons.push(n) } l.fitBounds(displayPolygonLayerGroup.getBounds()), currentSymbol = null }; export const _deleteAllPolygons = o => { _polygons.forEach(o => { displayPolygonLayerGroup.removeLayer(o) }), _polygons = [] };